# פרק 3: בסיס הנתונים (Database)

## תיאור כללי
מערכת Aruroa משתמשת במסד נתונים **MySQL** לאחסון כל המידע של המערכת. בסיס הנתונים מכיל **8 טבלאות** המאורגנות בצורה מנורמלת ומקושרות זו לזו באמצעות מפתחות זרים (Foreign Keys).

---

## תרשים DSD (קשרי גומלין)
**הנחיה:** צרף כאן את התמונה של התרשים ERD מקובץ `DATABASE_ERD.xml` (פתח ב-Draw.io, ייצא כתמונה והדבק כאן).

---

## פירוט טבלאות

### 1. טבלה: `users` (משתמשים)

**תפקיד הטבלה:**  
טבלה זו מאחסנת את כל המידע על המשתמשים במערכת - משתמשים רגילים ומנהלי מערכת.

**שדות הטבלה:**

| שם השדה | סוג נתון | תפקיד השדה |
|---------|----------|------------|
| `userid` | INT (PK) | מפתח ראשי - מזהה ייחודי לכל משתמש (AUTO_INCREMENT) |
| `username` | VARCHAR(50) | שם משתמש ייחודי להתחברות למערכת (UNIQUE) |
| `password` | VARCHAR(255) | סיסמה מוצפנת בשיטת SHA256 |
| `email` | VARCHAR(100) | כתובת דוא"ל ייחודית של המשתמש (UNIQUE) |
| `profilepicture` | LONGBLOB | תמונת פרופיל של המשתמש (אופציונלי) |
| `IsAdmin` | INT | הרשאת משתמש: 0 = משתמש רגיל, 1 = מנהל מערכת |
| `ResetCode` | VARCHAR(100) | קוד לאיפוס סיסמה (אופציונלי) |

**אינדקסים:**
- `idx_username` - לחיפוש מהיר לפי שם משתמש
- `idx_email` - לחיפוש מהיר לפי דוא"ל

---

### 2. טבלה: `songs` (שירים)

**תפקיד הטבלה:**  
טבלה זו מאחסנת את כל השירים שהועלו למערכת, כולל קובץ האודיו עצמו ומטא-דאטה.

**שדות הטבלה:**

| שם השדה | סוג נתון | תפקיד השדה |
|---------|----------|------------|
| `songid` | INT (PK) | מפתח ראשי - מזהה ייחודי לכל שיר (AUTO_INCREMENT) |
| `title` | VARCHAR(200) | שם השיר |
| `duration` | INT | משך השיר בשניות |
| `audioData` | LONGBLOB | קובץ האודיו עצמו (MP3/WAV/AAC) |
| `userid` | INT (FK) | מפתח זר - מזהה המשתמש שהעלה את השיר (קישור ל-`users.userid`) |
| `uploaded` | DATETIME | תאריך ושעת העלאה (ברירת מחדל: זמן נוכחי) |
| `plays` | INT | מספר ההשמעות של השיר (ברירת מחדל: 0) |

**אינדקסים:**
- `idx_title` - לחיפוש מהיר לפי שם שיר
- `idx_userid` - לשליפת כל השירים של משתמש מסוים
- `idx_plays` - למיון לפי פופולריות
- `idx_uploaded` - למיון לפי תאריך העלאה

**קשרים:**
- קשר **N:1** עם `users` - כל שיר שייך למשתמש אחד, משתמש יכול להעלות שירים רבים
- `ON DELETE CASCADE` - מחיקת משתמש תמחק את כל השירים שלו

---

### 3. טבלה: `genres` (ז'אנרים)

**תפקיד הטבלה:**  
טבלה זו מאחסנת את רשימת הז'אנרים המוזיקליים הזמינים במערכת.

**שדות הטבלה:**

| שם השדה | סוג נתון | תפקיד השדה |
|---------|----------|------------|
| `genreid` | INT (PK) | מפתח ראשי - מזהה ייחודי לכל ז'אנר (AUTO_INCREMENT) |
| `name` | VARCHAR(50) | שם הז'אנר (UNIQUE) - לדוגמה: Rock, Pop, Jazz |

**אינדקסים:**
- `idx_name` - לחיפוש מהיר לפי שם ז'אנר

**ז'אנרים קיימים במערכת:**  
Rock, Pop, Jazz, Classical, Electronic, Hip Hop, Country, Blues, Reggae, Metal, Folk, R&B, Soul, Funk, Disco, Techno, House, Trance, Dubstep, Ambient, Indie, Alternative, Punk, Grunge, Ska, Gospel, Latin, World, Experimental, Lo-Fi

---

### 4. טבלה: `song_genres` (קישור שירים-ז'אנרים) ⭐ **טבלת קישור**

**תפקיד הטבלה:**  
טבלת קישור המממשת קשר **רבים-לרבים (N:M)** בין שירים לז'אנרים. שיר אחד יכול להשייך למספר ז'אנרים, וז'אנר אחד יכול להכיל מספר שירים.

**שדות הטבלה:**

| שם השדה | סוג נתון | תפקיד השדה |
|---------|----------|------------|
| `songid` | INT (FK, PK) | מפתח זר וחלק ממפתח ראשי - מזהה השיר (קישור ל-`songs.songid`) |
| `genreid` | INT (FK, PK) | מפתח זר וחלק ממפתח ראשי - מזהה הז'אנר (קישור ל-`genres.genreid`) |

**מפתח ראשי מורכב:**  
`PRIMARY KEY (songid, genreid)` - מונע כפילויות של אותו שיר באותו ז'אנר

**אינדקסים:**
- `idx_songid` - לשליפת כל הז'אנרים של שיר מסוים
- `idx_genreid` - לשליפת כל השירים בז'אנר מסוים

**קשרים:**
- קשר **N:1** עם `songs` - `ON DELETE CASCADE`
- קשר **N:1** עם `genres` - `ON DELETE CASCADE`

**דוגמה:**  
אם שיר "Bohemian Rhapsody" משויך לז'אנרים Rock ו-Classical, יהיו 2 רשומות בטבלה:
- `(songid=1, genreid=1)` - Rock
- `(songid=1, genreid=4)` - Classical

---

### 5. טבלה: `playlists` (פלייליסטים)

**תפקיד הטבלה:**  
טבלה זו מאחסנת את הפלייליסטים שיצרו המשתמשים - רשימות שירים מותאמות אישית.

**שדות הטבלה:**

| שם השדה | סוג נתון | תפקיד השדה |
|---------|----------|------------|
| `playlistid` | INT (PK) | מפתח ראשי - מזהה ייחודי לכל פלייליסט (AUTO_INCREMENT) |
| `name` | VARCHAR(100) | שם הפלייליסט |
| `userid` | INT (FK) | מפתח זר - מזהה המשתמש שיצר את הפלייליסט (קישור ל-`users.userid`) |
| `ispublic` | BOOLEAN | האם הפלייליסט ציבורי (TRUE) או פרטי (FALSE) |
| `created` | DATETIME | תאריך ושעת יצירה (ברירת מחדל: זמן נוכחי) |

**אינדקסים:**
- `idx_userid` - לשליפת כל הפלייליסטים של משתמש מסוים
- `idx_name` - לחיפוש לפי שם פלייליסט

**קשרים:**
- קשר **N:1** עם `users` - כל פלייליסט שייך למשתמש אחד, משתמש יכול ליצור פלייליסטים רבים
- `ON DELETE CASCADE` - מחיקת משתמש תמחק את כל הפלייליסטים שלו

---

### 6. טבלה: `playlistsongs` (קישור פלייליסטים-שירים) ⭐ **טבלת קישור**

**תפקיד הטבלה:**  
טבלת קישור המממשת קשר **רבים-לרבים (N:M)** בין פלייליסטים לשירים. פלייליסט אחד יכול להכיל מספר שירים, ושיר אחד יכול להופיע במספר פלייליסטים. הטבלה גם שומרת את סדר השירים בפלייליסט.

**שדות הטבלה:**

| שם השדה | סוג נתון | תפקיד השדה |
|---------|----------|------------|
| `playlistid` | INT (FK, PK) | מפתח זר וחלק ממפתח ראשי - מזהה הפלייליסט (קישור ל-`playlists.playlistid`) |
| `songid` | INT (FK, PK) | מפתח זר וחלק ממפתח ראשי - מזהה השיר (קישור ל-`songs.songid`) |
| `position` | INT | מיקום השיר בפלייליסט (לשמירת סדר) |
| `dateadded` | DATETIME | תאריך ושעת הוספת השיר לפלייליסט (ברירת מחדל: זמן נוכחי) |

**מפתח ראשי מורכב:**  
`PRIMARY KEY (playlistid, songid)` - מונע הוספה כפולה של אותו שיר לאותו פלייליסט

**אינדקסים:**
- `idx_playlistid` - לשליפת כל השירים בפלייליסט מסוים
- `idx_position` - למיון השירים לפי סדר

**קשרים:**
- קשר **N:1** עם `playlists` - `ON DELETE CASCADE`
- קשר **N:1** עם `songs` - `ON DELETE CASCADE`

**דוגמה:**  
פלייליסט "My Favorites" מכיל 3 שירים:
- `(playlistid=1, songid=5, position=1)` - שיר ראשון
- `(playlistid=1, songid=12, position=2)` - שיר שני
- `(playlistid=1, songid=8, position=3)` - שיר שלישי

---

### 7. טבלה: `ratings` (דירוגים)

**תפקיד הטבלה:**  
טבלה זו מאחסנת את הדירוגים שמשתמשים נתנו לשירים (1-5 כוכבים).

**שדות הטבלה:**

| שם השדה | סוג נתון | תפקיד השדה |
|---------|----------|------------|
| `ratingid` | INT (PK) | מפתח ראשי - מזהה ייחודי לכל דירוג (AUTO_INCREMENT) |
| `userid` | INT (FK) | מפתח זר - מזהה המשתמש שנתן את הדירוג (קישור ל-`users.userid`) |
| `songid` | INT (FK) | מפתח זר - מזהה השיר שקיבל את הדירוג (קישור ל-`songs.songid`) |
| `rating` | INT | ערך הדירוג (1-5 כוכבים) - עם אילוץ CHECK |
| `daterated` | DATETIME | תאריך ושעת הדירוג (ברירת מחדל: זמן נוכחי) |

**אילוצים:**
- `CHECK (rating >= 1 AND rating <= 5)` - מבטיח שהדירוג בין 1 ל-5
- `UNIQUE KEY unique_user_song (userid, songid)` - משתמש יכול לדרג כל שיר פעם אחת בלבד

**אינדקסים:**
- `idx_songid` - לחישוב ממוצע דירוג לשיר
- `idx_rating` - לסינון לפי דירוג

**קשרים:**
- קשר **N:1** עם `users` - משתמש יכול לדרג שירים רבים
- קשר **N:1** עם `songs` - שיר יכול לקבל דירוגים ממשתמשים רבים
- `ON DELETE CASCADE` - מחיקת משתמש או שיר תמחק את הדירוגים הקשורים

---

### 8. טבלה: `genre_requests` (בקשות ז'אנרים)

**תפקיד הטבלה:**  
טבלה זו מאחסנת בקשות של משתמשים להוספת ז'אנרים חדשים למערכת. מנהל המערכת יכול לאשר או לדחות בקשות אלו.

**שדות הטבלה:**

| שם השדה | סוג נתון | תפקיד השדה |
|---------|----------|------------|
| `requestid` | INT (PK) | מפתח ראשי - מזהה ייחודי לכל בקשה (AUTO_INCREMENT) |
| `userid` | INT (FK) | מפתח זר - מזהה המשתמש שביקש את הז'אנר (קישור ל-`users.userid`) |
| `genre_name` | VARCHAR(50) | שם הז'אנר המבוקש |
| `requested_date` | DATETIME | תאריך ושעת הבקשה (ברירת מחדל: זמן נוכחי) |
| `status` | VARCHAR(20) | סטטוס הבקשה: 'pending' (ממתין), 'approved' (אושר), 'rejected' (נדחה) |
| `reviewed_by` | INT (FK) | מפתח זר - מזהה המנהל שטיפל בבקשה (קישור ל-`users.userid`, אופציונלי) |
| `reviewed_date` | DATETIME | תאריך ושעת הטיפול בבקשה (אופציונלי) |

**אינדקסים:**
- `idx_status` - לסינון בקשות לפי סטטוס
- `idx_userid` - לשליפת כל הבקשות של משתמש מסוים

**קשרים:**
- קשר **N:1** עם `users` (userid) - משתמש יכול לבקש ז'אנרים רבים
- קשר **N:1** עם `users` (reviewed_by) - מנהל יכול לטפל בבקשות רבות
- `ON DELETE CASCADE` על userid - מחיקת משתמש תמחק את בקשותיו
- `ON DELETE SET NULL` על reviewed_by - מחיקת מנהל לא תמחק את הבקשות שטיפל בהן

---

## סיכום קשרים בין הטבלאות

### קשרים מסוג 1:N (אחד לרבים)
1. **users → songs** - משתמש מעלה שירים רבים
2. **users → playlists** - משתמש יוצר פלייליסטים רבים
3. **users → ratings** - משתמש מדרג שירים רבים
4. **users → genre_requests** - משתמש מבקש ז'אנרים רבים
5. **songs → ratings** - שיר מקבל דירוגים רבים

### קשרים מסוג N:M (רבים לרבים) - דרך טבלאות קישור
1. **songs ↔ genres** (דרך `song_genres`) - שיר יכול להשייך למספר ז'אנרים, ז'אנר מכיל מספר שירים
2. **playlists ↔ songs** (דרך `playlistsongs`) - פלייליסט מכיל מספר שירים, שיר יכול להופיע במספר פלייליסטים

### קשר עצמי (Self-Referencing)
- **users → genre_requests (reviewed_by)** - משתמש (מנהל) מאשר בקשות של משתמשים אחרים

---

## אבטחת מידע

### הצפנת סיסמאות
- כל הסיסמאות מוצפנות באמצעות **SHA256** לפני השמירה במסד הנתונים
- אין אפשרות לשחזר את הסיסמה המקורית מהמסד נתונים

### הגנה מפני SQL Injection
- כל השאילתות במערכת משתמשות ב**פרמטרים** (Parameterized Queries)
- אין שרשור ישיר של קלט משתמש לשאילתות SQL

### Foreign Keys עם CASCADE
- מחיקת משתמש תמחק אוטומטית את כל השירים, הפלייליסטים והדירוגים שלו
- מחיקת שיר תמחק אוטומטית את כל הקישורים שלו לפלייליסטים וז'אנרים

---

## שאילתות שימושיות

### סטטיסטיקות כלליות
```sql
SELECT 
    (SELECT COUNT(*) FROM users) as total_users,
    (SELECT COUNT(*) FROM songs) as total_songs,
    (SELECT COUNT(*) FROM playlists) as total_playlists,
    (SELECT COALESCE(SUM(plays), 0) FROM songs) as total_plays;
```

### 10 השירים הכי פופולריים
```sql
SELECT s.songid, s.title, s.plays, u.username as uploader
FROM songs s
JOIN users u ON s.userid = u.userid
ORDER BY s.plays DESC
LIMIT 10;
```

### שירים עם הז'אנרים שלהם
```sql
SELECT s.title, GROUP_CONCAT(g.name SEPARATOR ', ') as genres
FROM songs s
LEFT JOIN song_genres sg ON s.songid = sg.songid
LEFT JOIN genres g ON sg.genreid = g.genreid
GROUP BY s.songid, s.title;
```

### ממוצע דירוג לכל שיר
```sql
SELECT s.title, AVG(r.rating) as avg_rating, COUNT(r.ratingid) as num_ratings
FROM songs s
LEFT JOIN ratings r ON s.songid = r.songid
GROUP BY s.songid, s.title
HAVING num_ratings > 0
ORDER BY avg_rating DESC;
```

---

## נורמליזציה

בסיס הנתונים מנורמל ברמה **3NF (Third Normal Form)**:

✅ **1NF** - כל השדות מכילים ערכים אטומיים (לא רשימות)  
✅ **2NF** - כל השדות תלויים במפתח הראשי המלא  
✅ **3NF** - אין תלות טרנזיטיבית בין שדות

**דוגמה לנורמליזציה:**  
במקום לשמור רשימת ז'אנרים כטקסט בטבלת `songs` (למשל: "Rock, Pop, Jazz"), יצרנו טבלת קישור `song_genres` שמאפשרת קשר רבים-לרבים תקני.

---

**סיום פרק 3**
